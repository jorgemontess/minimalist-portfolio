---
//Se ejecuta en el servidor o build time
const { title } = Astro.props
---

<section>
    {title && <h2>{title}</h2>}
    <slot /> <!--Renderisa los elementos que se colocan en el section
                Es com oun hueco donde colocamos informacion -->
</section>


<style>
    section {
        max-width: 700px;
        margin: 0 auto 72px; <!--0 Margen Arriba, auto para centrar, separacion abajo 72px -->
    }

    h2 {
        margin-bottom: 8px;
        font-weight: 700;
        line-height: 1.5;
        font-size: 1.5rem;

    }
</style>